{
  "name": "family-flight-tracker",
  "version": "1.0.0",
  "description": "Family flight schedule tracker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build step required'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
